interface RowType {
  name: string;
  id: string;
  city: string;
  person: string;
  tel: string;
  fast: string;
  slow: string;
  status: number;
  now: string;
  fault: string;
}

export type { RowType };
